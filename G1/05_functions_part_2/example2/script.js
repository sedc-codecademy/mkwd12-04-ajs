"use strict";
// use strict mode to prevent bad syntax and other errors to be executed in the browser without notice (e.g. undeclared variables)

// Strict JS

// number = 1;
let number = 1;

// let eval = () => 2;
// let arguments = 3;
